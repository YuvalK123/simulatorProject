//
// Created by yuval Kasner on 20/12/19.
//

#include "IfCommand.h"
int IfCommand::execute(vector<string>::iterator it) {
  int retValue = 1, tmp = 0;
//  initiateCond(vec,index);
//  if(condition){
//    for(Command* c :commands){
//      tmp = c->execute(vec,index);
//      retValue += tmp;
//      index += tmp;
//    }
//  }
  return retValue;
}